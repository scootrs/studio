import React from 'react';

import { Button } from 'shared/styles/input/button';

function ApplicationSaveButtonView({ onClick }) {
  return <Button onClick={onClick}>Save</Button>;
}
export default ApplicationSaveButtonView;
